name: "Security"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '44 19 * * 2'
  workflow_dispatch: 

jobs:
  changes:
    name: Detect security-relevant changes
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      source: ${{ steps.filter.outputs.source }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Detect security-relevant changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            source:
              - '.github/**'
              - 'app/**'
              - 'core/**'
              - 'config/**'
              - 'gradle/**'
              - 'build.gradle.kts'
              - 'settings.gradle.kts'
              - 'gradle.properties'
              - 'gradlew'
              - 'gradlew.bat'

  analyze:
    name: Analyze (${{ matrix.language }})
    needs: changes
    if: >-
      always() &&
      (github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.source == 'true')
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    timeout-minutes: 60
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: java-kotlin
            build-mode: manual
          - language: swift
            build-mode: manual

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # 1. Prepare environment ONLY for Kotlin/Java compilation
      - name: Set up JDK 21
        if: matrix.language == 'java-kotlin'
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Gradle Cache
        if: matrix.language == 'java-kotlin'
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-disabled: true

      # 2. Initialize CodeQL
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          tools: linked

      # hopefully this caching strat improves ci time
      - name: Cache Gradle & Konan (Swift job)
        if: matrix.language == 'swift'
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.konan
          key: ${{ runner.os }}-konan-gradle-${{ hashFiles('**/*.gradle.kts', 'gradle/libs.versions.toml', 'gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-konan-gradle-

      # 3. Compile Step
      - name: Run manual build steps
        if: matrix.build-mode == 'manual'
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ matrix.language }}" = "java-kotlin" ]; then
            chmod +x gradlew
            ./gradlew clean \
              :core:compileKotlinJvm \
              :core:compileAndroidMain \
              :app:sharedLogic:compileAndroidMain \
              :app:sharedUI:compileAndroidMain \
              :app:androidApp:compileDebugKotlin \
              --stacktrace --no-daemon --no-build-cache --rerun-tasks
          elif [ "${{ matrix.language }}" = "swift" ]; then
            xcodebuild \
              -project app/iosApp/iosApp.xcodeproj \
              -scheme iosApp \
              -sdk iphonesimulator \
              -configuration Debug \
              -derivedDataPath build/codeql-ios-derived-data \
              CODE_SIGNING_ALLOWED=NO \
              build
          else
            echo "No manual build needed for ${{ matrix.language }}"
          fi

      # 4. Perform CodeQL Analysis
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{matrix.language}}"

  trivy-sca:
    name: Dependency Scan (Trivy)
    needs: changes
    if: >-
      always() &&
      (github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.source == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-disabled: true

      - name: Generate Android release SBOM
        run: >-
          ./gradlew :app:androidApp:cyclonedxDirectBom
          --stacktrace --no-daemon --no-configuration-cache

      - name: Validate Android release SBOM
        shell: bash
        run: |
          sbom_path='build/reports/sbom/android-release.cdx.json'
          if [[ ! -s "$sbom_path" ]]; then
            echo "::error file=$sbom_path::CycloneDX SBOM is missing or empty"
            exit 1
          fi
          if ! component_count="$(jq -er '
            select(.bomFormat == "CycloneDX")
            | (.components // [])
            | select(length > 0)
            | length
          ' "$sbom_path")"; then
            echo "::error file=$sbom_path::CycloneDX SBOM is invalid or has no components"
            exit 1
          fi
          echo "CycloneDX SBOM components: $component_count"

      - name: Preserve Android release SBOM
        uses: actions/upload-artifact@v7
        with:
          name: android-release-cyclonedx-sbom
          path: build/reports/sbom/android-release.cdx.json
          if-no-files-found: error
          retention-days: 14

      - name: Run Trivy vulnerability scanner
        id: trivy
        continue-on-error: true
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          version: 'v0.70.0'
          scan-type: 'sbom'
          scan-ref: 'build/reports/sbom/android-release.cdx.json'
          scanners: 'vuln'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'HIGH,CRITICAL'
          limit-severities-for-sarif: true
          exit-code: '1'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('trivy-results.sarif') != ''
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'trivy-sca'

      - name: Preserve Trivy SARIF report
        uses: actions/upload-artifact@v7
        if: always() && hashFiles('trivy-results.sarif') != ''
        with:
          name: trivy-results-sarif
          path: trivy-results.sarif
          retention-days: 14

      - name: Enforce Trivy result
        if: always() && steps.trivy.outcome == 'failure'
        shell: bash
        run: |
          echo "::error::Trivy found a HIGH or CRITICAL vulnerability, or the SBOM scan failed"
          exit 1
